{% extends 'base.html.twig' %}

{% block body %}
<h1 class="text-2xl mb-4">Utilisateurs</h1>

<a href="{{ path('admin_user_new') }}" class="btn btn-primary mb-3">Ajouter</a>

<table class="table">
    <thead>
        <tr>
            <th>Email</th>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Rôles</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.email }}</td>
                <td>{{ user.lastname }}</td>
                <td>{{ user.firstname }}</td>
                <td>{{ user.roles|join(', ') }}</td>
                <td>
                    <a href="{{ path('admin_user_edit', {id: user.id}) }}">✏️</a>
                    <a href="{{ path('admin_user_delete', {id: user.id}) }}"
                       onclick="return confirm('Supprimer ?')">🗑️</a>
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
