<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">

    <title>{% block title %}Backoffice{% endblock %}</title>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    {# Tailwind CSS compilé via symfonycasts/tailwind-bundle #}
    <link rel="stylesheet" href="{{ asset('build/app.css') }}">

    {% block stylesheets %}{% endblock %}
</head>

<body class="bg-gray-100 min-h-screen flex">

    {# ================= MENU GAUCHE ================= #}
    <aside class="w-64 bg-gray-800 text-white flex flex-col">
        <div class="p-6 text-xl font-bold border-b border-gray-700">
            Backoffice
        </div>

        <nav class="flex-1 p-4 space-y-2">
            <a href="{{ path('app_dashboard') }}"
               class="block px-4 py-2 rounded hover:bg-gray-700 transition">
                📊 Dashboard
            </a>

            {# Visible pour tous les utilisateurs connectés #}
            <a href="{{ path('product_index') }}"
               class="block px-4 py-2 rounded hover:bg-gray-700 transition">
                📦 Produits
            </a>

            {# Visible ADMIN + MANAGER #}
            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MANAGER') %}
                <a href="{{ path('client_index') }}"
                   class="block px-4 py-2 rounded hover:bg-gray-700 transition">
                    👥 Clients
                </a>
            {% endif %}

            {# Visible ADMIN uniquement #}
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('user_index') }}"
                   class="block px-4 py-2 rounded hover:bg-gray-700 transition">
                    🔐 Utilisateurs
                </a>
            {% endif %}
        </nav>

        {# ================= FOOTER MENU ================= #}
        <div class="p-4 border-t border-gray-700">
            {% if app.user %}
                <div class="text-sm mb-2">
                    Connecté en tant que<br>
                    <strong>{{ app.user.firstname }} {{ app.user.lastname }}</strong>
                </div>

                <a href="{{ path('app_logout') }}"
                   class="block text-center bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded transition">
                    🚪 Déconnexion
                </a>
            {% endif %}
        </div>
    </aside>

    {# ================= CONTENU PRINCIPAL ================= #}
    <main class="flex-1 p-6">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="mb-4 px-4 py-2 rounded
                    {% if label == 'success' %}bg-green-100 text-green-800{% endif %}
                    {% if label == 'error' %}bg-red-100 text-red-800{% endif %}
                ">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}

        {% block body %}{% endblock %}
    </main>

    {% block javascripts %}{% endblock %}
</body>
</html>
